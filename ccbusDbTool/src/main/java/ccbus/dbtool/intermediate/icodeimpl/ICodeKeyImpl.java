package ccbus.dbtool.intermediate.icodeimpl;

import ccbus.dbtool.intermediate.ICodeKey;

public enum ICodeKeyImpl implements ICodeKey
{
    LINE, ID, VALUE, NAME, NAME_LAST, VALUE_RAW,
    KEY, SEPARATOR, QUERY_NAME,
    TYPE,TYPE_LAST, TEMPLATE_TYPE, CLASS_TYPE, JOIN_TYPE, BASE_NAME,
    ARRAY_ELEMENT_TYPE, MODIFIER;

}
